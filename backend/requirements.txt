# ─── Core Framework ───────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.3.0

# ─── Supabase ─────────────────────────────────────────────────────────────────
supabase>=2.4.0,<2.20.0

# ─── Google Gemini (Q&A answer generation — lightweight HTTP) ─────────────────
google-genai>=0.1.0

# ─── Image processing (thumbnail generation only — no heavy ML) ───────────────
Pillow>=10.3.0

# ─── HTTP client (calls HuggingFace Space AI Worker) ─────────────────────────
httpx>=0.27.0

# ─── Rate limiting ────────────────────────────────────────────────────────────
slowapi>=0.1.9

# ─── Payments (optional — endpoints return 503 if STRIPE_SECRET_KEY empty) ───
stripe>=8.0.0

# ─── Multi-replica state (optional — set REDIS_URL to activate) ──────────────
# Powers cross-replica cancellation, event-bus replay, and rate-limit storage.
# Without REDIS_URL the code transparently falls back to in-memory dicts.
redis>=5.0.0

# ─── DB migrations (used by app.scripts.apply_migrations preDeployCommand) ───
psycopg[binary]>=3.1.0

# ─── HTML parsing (manga chapter scraper) ────────────────────────────────────
beautifulsoup4>=4.12.0
lxml>=5.2.0               # Fast BS4 parser

# ─── Utilities ────────────────────────────────────────────────────────────────
python-multipart>=0.0.9   # FastAPI file uploads
python-dotenv>=1.0.0

# ─── Observability ────────────────────────────────────────────────────────────
sentry-sdk[fastapi]>=2.0.0
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
opentelemetry-instrumentation-fastapi>=0.41b0
opentelemetry-instrumentation-httpx>=0.41b0
opentelemetry-exporter-otlp-proto-http>=1.20.0

# ─── Testing ──────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
respx>=0.21.0
httpx[http2]>=0.27.0

# NOTE: Heavy ML deps (sentence-transformers, torch, ultralytics, manga-ocr) are NOT here.
# They run on HuggingFace Spaces (16 GB RAM).
