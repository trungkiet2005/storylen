# ─── Core Framework ───────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.3.0

# ─── Supabase ─────────────────────────────────────────────────────────────────
supabase>=2.4.0

# ─── Google Gemini (Q&A answer generation — lightweight HTTP) ─────────────────
google-genai>=0.1.0

# ─── Image processing (thumbnail generation only — no heavy ML) ───────────────
Pillow>=10.3.0

# ─── HTTP client (calls HuggingFace Space AI Worker) ─────────────────────────
httpx>=0.27.0

# ─── Utilities ────────────────────────────────────────────────────────────────
python-multipart>=0.0.9   # FastAPI file uploads
python-dotenv>=1.0.0

# NOTE: sentence-transformers, torch, ultralytics, manga-ocr are NOT here.
# They live in ai_service/requirements.txt and run on HuggingFace Spaces (16 GB RAM).
